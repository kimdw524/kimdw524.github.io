import * as React from 'react';
import type { HeadFC, PageProps } from 'gatsby';

const Index = (props: PageProps) => {
  return <></>;
};

export default Index;

export const Head: HeadFC = () => <title>kimdw524's blog</title>;
